<!-- markdownlint-disable MD013 MD060 -->

# Implementation Plans

Generated by the improve skill on 2026-06-28 at commit `b6f26a6`; updated on 2026-06-30 at commit `4b5cba0` with follow-up plans; updated on 2026-07-18 at commit `f7f0e9d` with design/UI/UX review plans (007–012). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bundle the CMS admin runtime into audited project assets | P1 | M | — | DONE |
| 002 | Enforce the site Content Security Policy | P1 | M | 001 | DONE |
| 003 | Emit one canonical URL on paginated blog pages | P1 | S | — | DONE |
| 004 | Run the trigger-build auth regression in CI | P2 | S | — | DONE |
| 005 | Keep CMS field options and authoring helpers in sync with the Astro content schema | P1 | M | — | DONE |
| 006 | Remove stale Keystatic / Astro 6 documentation left after the Sveltia migration | P2 | S | — | DONE |
| 007 | モバイルの記事ページ横スクロール（レイアウト崩れ）を修正する | P1 | S | — | DONE |
| 008 | axe が検出する color-contrast (serious) 違反を解消する | P2 | S | — | TODO |
| 009 | ブログ一覧の絞り込みを全記事対象にする | P2 | M | 007 | TODO |
| 010 | ダークテーマでの画像のまぶしさを抑える | P3 | S | — | TODO |
| 011 | 旧記事のヒーロー画像重複と裸 URL を整理する | P3 | M | — | TODO |
| 012 | 小粒 UI 改善スイープ（/now 導線・ヘッダー狭幅・共有 URL ほか） | P3 | S | 007 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 002 depends on Plan 001 because the current CSP allows `https://unpkg.com` and `https://esm.sh` for `/admin`; enforcing CSP before bundling the CMS runtime would likely break the admin UI or require keeping obsolete CDN script allowances.
- Plans 003 and 004 are independent and can run before or after 001/002.
- Plan 005 is the highest-leverage follow-up because it prevents invalid authoring suggestions and future CMS/schema drift.
- Plan 006 is independent and text-only; it can be done before or after Plan 005.
- Plan 007 is the highest-priority new finding: every blog post with a code block horizontally overflows on mobile (verified `scrollWidth` 1026–1546px at a 390px viewport). Execute it first among 007–012.
- Plans 009 and 012 touch `src/pages/blog/[...page].astro` / `Header.astro` after Plan 007 does; run them after 007 to avoid conflicts.
- Plans 008, 010, 011 are independent of each other and of 007.

## Verification baseline from audit

These read-only checks were run during planning:

- 2026-06-28: `pnpm exec astro check` → 0 errors, 1 hint (`src/pages/admin.astro` inline script hint)
- 2026-06-28: `pnpm run test:unit` → 23 tests passed
- 2026-06-28: `pnpm run lint` → pass
- 2026-06-28: `pnpm run lint:alt` → pass
- 2026-06-28: `pnpm audit --registry=https://registry.npmjs.org/ --audit-level=high` → no known vulnerabilities
- 2026-06-28: LSP diagnostics for `src/` → no diagnostics
- 2026-06-30: `pnpm exec astro check` → 0 errors, 0 warnings, 0 hints
- 2026-06-30: `pnpm run test:unit` → 23 tests passed
- 2026-06-30: `pnpm run lint:alt` → pass
- 2026-06-30: `pnpm audit --registry=https://registry.npmjs.org/ --audit-level=high` → no known vulnerabilities
- 2026-07-18 (design/UI/UX review): `pnpm run build` → success (astro check 0 errors, 101 pages indexed by Pagefind)
- 2026-07-18: full-page screenshots (light/dark × desktop/mobile) of `/`, `/blog/`, blog posts, `/works/`, `/about/`, `/now/`, `/colophon/`, `/404` → sources for Plans 007/010/011/012
- 2026-07-18: axe (wcag2a/2aa/21aa/22aa) on built site, light/dark × 5 pages → only color-contrast (serious) violations → Plan 008
- 2026-07-18: mobile (390px) overflow probe → post pages scrollWidth 1026–1546px, home 414px → Plan 007

## Findings considered and rejected

- `BaseHead.astro` JSON-LD uses `set:html`, but the payload is produced with `JSON.stringify(...)` from structured objects, so it was not treated as an actionable XSS finding.
- `/api/trigger-build` bearer-token comparison itself was not treated as a vulnerability: the route has a production fail-fast when `CRON_SECRET` is missing and returns before reading the deploy hook on wrong tokens. The actionable gap is CI coverage, captured in Plan 004.
- Pagefind search was not reported as untested because `tests/playwright/blog-search.spec.ts` and `tests/playwright/pagefind-index.spec.ts` already cover search UI and build artifact generation.
- A full documentation sync from Astro 6 to Astro 7 was previously deferred; it is now covered by Plan 006.
- Local `.env` contains legacy Keystatic environment variable names. Because this repository is public and `.env` may contain sensitive local values, do not publish those locations in a public GitHub issue without explicit user confirmation. Plan 006 cleans `.env.example`; local `.env` cleanup/rotation should be handled privately if those values are real credentials.

## Deferred GitHub issues

- CI/local verification script ergonomics: add package scripts for E2E, Lighthouse CI, and linkcheck so developers can run CI-equivalent checks locally without copying commands from `.github/workflows/ci.yml`. Tracked in <https://github.com/keroway/astro-blog/issues/442>. Deferred because it is useful but not blocking.

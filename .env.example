# Keystatic 環境変数サンプル
# 本ファイルは git 管理下、シークレットを直書きしないこと。
# 実値は .env (gitignore 済み) または Vercel 環境変数に設定する。
# セットアップ手順: docs/cms-flow.md

# ---- Keystatic storage mode ----
# 本番 (Vercel) では "github" を設定して GitHub mode (branch storage) を有効化する。
# 未設定 / "local" のときは ./src/content/* に直接書き込むローカルモードで動く。
# Vercel Production (VERCEL_ENV=production) では "github" 必須。
# 未設定だと astro.config.mjs がビルド時に fail-fast する。
# Preview デプロイは Keystatic 統合自体が mount されない (/keystatic は 404) ので
# env を増やす必要なし。Local dev は local モードで OK。
#
# 注意: keystatic.config.ts はブラウザにも bundle されるため、process.env ではなく
# import.meta.env.PUBLIC_* で読む必要がある。そのため PUBLIC_ プレフィックスが必須。
# storage 種別は Admin UI から観測可能なので秘匿不要。
PUBLIC_KEYSTATIC_STORAGE_KIND=local

# 注: storage=github のリポジトリ (owner=keroway / name=astro-blog) は
# keystatic.config.ts にハードコードしている。フォーク等で変更する場合は
# そちらを直接編集する (env では切り替えない)。

# ---- Keystatic GitHub App ----
# https://keystatic.com/docs/github-mode の手順で生成される値。
# CLIENT_SECRET と KEYSTATIC_SECRET はシークレット扱い。
KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=

# Astro はビルド時に "PUBLIC_" 接頭辞の env のみクライアントに露出する。
# Keystatic Admin UI が GitHub App をインストールするボタンの遷移先に使うため公開して問題ない。
PUBLIC_KEYSTATIC_GITHUB_APP_SLUG=

# ---- 公開予約 / ビルドフック ----
# Vercel ダッシュボード → Project → Settings → Git → Deploy Hooks で生成した URL。
# /api/trigger-build が毎日 UTC 0:00 にこの URL を POST してリビルドをトリガーする。
# Production 環境変数として登録すること。
VERCEL_DEPLOY_HOOK_URL=

# Vercel Cron Jobs が /api/trigger-build を呼ぶときに付与する Bearer トークン。
# openssl rand -hex 32 などで生成し、Vercel 環境変数と .env の両方に設定する。
# 未設定の場合は認証をスキップする（ローカル開発向け）。
CRON_SECRET=
